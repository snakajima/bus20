{
  "name": "@bus20/graph",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic directed-graph primitives (shortest paths, reachability) for the Bus 2.0 benchmark",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./dist/src/*.d.ts",
      "default": "./dist/src/*.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bus20/contracts": "0.1.0"
  }
}
